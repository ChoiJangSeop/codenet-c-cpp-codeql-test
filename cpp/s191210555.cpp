#include <bits/stdc++.h>
#include <math.h>
using namespace std;

int main(){
    double r;
    cin >> r;
    double s, l;
    s = M_PI * r * r;
    l = 2 * M_PI * r;
    printf("%f %f\n", s, l);
    return 0;
}
