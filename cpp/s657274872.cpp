#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for   (int i = 0; i < (int)(n); i++)
typedef long long ll;
int main() {
    // ifstream in("data1.txt");
    // cin.rdbuf(in.rdbuf());
    double N;
    cin >> N;
    double pi=3.1415926535898;
    printf("%.12f %.12f",  N * N * pi, (N + N) * pi);
}

